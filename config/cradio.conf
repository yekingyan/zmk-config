# Ferris Sweep (cradio) 配置
# 从 Lily58 同步的经过验证的蓝牙稳定性配置

# Keyboard Name
CONFIG_ZMK_KEYBOARD_NAME="Sweep_OSM"

# --- 蓝牙稳定性优化 ---
# 克隆板/SuperMini 禁用 2M PHY，强制使用更稳的 1M PHY
CONFIG_BT_CTLR_PHY_2M=n

# 增强发射功率
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# 实验性蓝牙连接修复
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# 实验性安全配对（解决 Win11 拒绝配对 Err 9）
CONFIG_ZMK_BLE_EXPERIMENTAL_SEC=y

# 强制内部 RC 振荡器（克隆板无外部晶振，解决 0x22 LMP Response Timeout）
CONFIG_CLOCK_CONTROL_NRF_K32SRC_RC=y

# --- Windows 11 专项修复 ---
# 绕过 Win11 GATT 电量通知断联 Bug
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n

# 实验性特性会导致 Win11 断流 0x22 timeout，关闭
CONFIG_ZMK_BLE_EXPERIMENTAL_FEATURES=n

# 开启安全配对码
CONFIG_ZMK_BLE_PASSKEY_ENTRY=y

# Battery reporting
CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_ZMK_BATTERY_REPORT_INTERVAL=60

# 深度睡眠 15 分钟
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# 增加蓝牙 L2CAP 缓冲区
CONFIG_BT_L2CAP_TX_BUF_COUNT=8
CONFIG_BT_L2CAP_TX_MTU=65

# 扩大内存栈
CONFIG_MAIN_STACK_SIZE=2048
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=2048

# 请求更短连接间隔（高频输入稳定性）
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=12
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=24

# 蓝牙超时延长到 8 秒（容忍 Windows 偶尔卡顿）
CONFIG_BT_PERIPHERAL_PREF_TIMEOUT=800

# 工作队列容量
CONFIG_ZMK_BEHAVIORS_QUEUE_SIZE=512
